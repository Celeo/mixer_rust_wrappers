appenders:
  stdout:
    kind: console
    encoder:
      pattern: "{d} {h({l})} {m}{n}"
root:
  level: warn
  appenders:
    - stdout
loggers:
  mixer_rust_wrappers:
    level: debug
  driver:
    level: debug
